/*
Package calendar provides a library used to map out various offsets
*/
package calendar

// Calendar is the main struct that is used to store calendar data.
// Should be used via constructor:
//	calendar.New()
type Calendar struct {
}

// New is the constructor for Calendar.
func New() (new_calendar Calendar) {
	return
}